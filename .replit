language = "python3"
run = "streamlit run app.py --server.port=${PORT:-8501} --server.address=0.0.0.0"
modules = ["python-3.12"]

[deployment]
run = ["sh", "-c", "streamlit run app.py --server.port=${PORT:-8501} --server.address=0.0.0.0"]

[nix]
channel = "stable-24_05"
